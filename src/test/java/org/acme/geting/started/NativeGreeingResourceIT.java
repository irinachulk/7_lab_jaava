package org.acme.geting.started;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeGreeingResourceIT extends GreeingResourceTest {

    // Execute the same tests but in native mode.
}